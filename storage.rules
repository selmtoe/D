rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // v2 currently stores avatar configuration, not user-uploaded binaries.
    // Keep Storage closed until a bounded upload schema and moderation policy
    // are approved.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
